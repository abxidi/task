# 交接包验证记录

日期：2026-07-13

## 已验证

### 设计一致性

- 产品规格、UI 规范、根目录规则和启动提示均使用 `-3...3`。
- 两个维度、49 个坐标、正方形地图、颜色/数字编码和零轴分类一致。
- 任务内容优先、设置收纳属性、四象限置顶的结论一致。
- 本机存储、AI 可选、变更先审阅和看板完成列规则一致。
- 四份实施计划按依赖顺序覆盖规格中的基础、核心任务、看板指标、AI、提醒、备份和发布。

### 文档结构

- 所有交接索引文件存在且非空。
- 所有 Markdown code fence 成对闭合。
- 未发现未决标记、未填写章节或“以后实现”占位。
- `git diff --check` 未发现尾随空格或冲突标记。

### UI 参考静态检查

对 `docs/ui/task-ui-reference.html` 执行：

- HTML 标签栈配对检查：通过。
- 18 个 HTML ID 唯一性检查：通过。
- JavaScript `new Function` 语法解析：通过。
- CSS 花括号平衡检查：通过。
- 关键交互节点检查：三个视图、AI toggle、任务设置 toggle、主地图和可拖拽任务点均存在。
- 紧急度 `-3...3` 七个 `data-u` 值均存在且互不合并。

### 官方资料

- 交接索引中的核心 Apple Developer Documentation 页面返回 HTTP 200。
- OpenAI Developer Documentation 在当前网络环境返回 HTTP 403，未把其内容标记为已读取。

## 当前限制

应用内浏览器安全策略拒绝直接打开本地 `file://` 页面，因此本次未生成自动浏览器截图，也未在自动浏览器中执行拖拽。没有通过 localhost 或其他浏览器绕过该限制。

接手 Agent 在实现 UI 前应直接打开：

```text
docs/ui/task-ui-reference.html
```

并人工确认三个视图可以切换、主任务点可以拖动和用方向键移动、任务设置可以展开、AI toggle 可以移除看板 AI 面板。原生 SwiftUI 实现完成后必须使用真实 macOS 运行态重新做视觉与可访问性验证。

## 读者检查结果

`docs/handoff/READER_CHECKLIST.md` 中 13 个问题均能在指定来源找到唯一答案。未来若修改规格，应同步更新 UI 规范、实施计划、启动提示和该检查表。
